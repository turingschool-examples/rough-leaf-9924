<h1> All Items </h1>

<% @items.each do |item| %>
    <div id="item-<%= item.id %>">
        <%= item.name %>
        $<%= item.price %>
        <%= item.supermarket.name %>
        Customers: <%= item.customer_count%>
    </div>
<% end %>