<h1><%= @customer.name %></h1>

<h2>Items</h2>
<ul class="items">
  <% @customer.items.each do |item| %>
    <li>
      <%= item.name %> - $<%= item.price %> - <%= item.supermarket.name %>
    </li>
  <% end %>
</ul>
